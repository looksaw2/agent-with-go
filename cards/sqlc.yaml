
version: "2"
sql:
  - engine: "postgresql"
    queries: "./sqlc/queries/todo.sql"
    schema: "./sqlc/schema/todo.sql"
    gen:
      go:
        package: "db"
        out: "./internal/db"
        sql_package: "pgx/v5"
        emit_interface: true